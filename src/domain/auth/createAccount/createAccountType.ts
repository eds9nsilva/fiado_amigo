export interface User {
    name: string;
    email: string;
}

export interface ParamsCreateAccount {
    name: string;
    email: string;
    password: string;
}

export interface UserAPI {
    name: string;
    email: string;
}
